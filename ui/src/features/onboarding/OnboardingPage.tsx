/** Stub (T10) — guided in-browser onboarding is a later increment; for now
 * new profiles are set up via the `/setup` wizard in Claude Code. */
export function OnboardingPage() {
  return (
    <div className="p-6 text-muted-foreground">
      Coming soon — guided profile onboarding in the browser. For now, run the /setup
      wizard in Claude Code.
    </div>
  );
}
