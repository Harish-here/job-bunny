/** Stub (T10) — run stats, funnel drops, and match-quality trends are a
 * later increment; this keeps the nav item real without pretending the
 * feature exists yet. */
export function AnalyticsPage() {
  return (
    <div className="p-6 text-muted-foreground">
      Coming soon — run stats, funnel drops, and match-quality trends will land here.
    </div>
  );
}
