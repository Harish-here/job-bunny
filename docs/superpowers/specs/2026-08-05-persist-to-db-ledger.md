# Persist-to-DB: Advisor Decision Ledger

Running log of in-flight advisor decisions during the unattended 2026-08-05→06 execution of all four phases (user grant: "you can make judgement in flight and make ledger for it"). Newest entries appended at the bottom.

| # | When | Decision | Rationale |
|---|---|---|---|
| L1 | 2026-08-05 | Integration branch named `main-everything-db`, cut from fresh `origin/main`. | User's message had two spellings; took the corrected one. Fresh fetch per the stale-base worktree lesson. |
| L2 | 2026-08-05 | Each phase lands as a PR targeting `main-everything-db`, merged by the advisor when its gates pass (review wave + `npm run check` + live rajni verification). Merge of `main-everything-db` → `main` is deferred to the user after stability. | User instruction: "push all PR to 'main-everything-db'"; landing on main stays a human decision. |
| L3 | 2026-08-05 | All four phases run tonight; Phase 2–4 specs are written just-in-time, informed by the previous landed phase. | User overrode the phase-1-only recommendation; just-in-time spec order preserved within the night. |
| L4 | 2026-08-05 | Live verification = `profiles/rajni/` via the verify skill only; `profiles/harish/` is never touched. | CLAUDE.md hard rule. |
| L5 | 2026-08-05 | CLAUDE.md edits: the three Phase-1 edits are pre-approved verbatim (advisor session). Phase 4's CLAUDE.md changes (incl. the board hard-rule amendment) land only if the user approves the shown text before going offline; otherwise they ship as a proposed-diff file in the Phase 4 PR. | Standing rule: no instruction-file edits without verbatim approval. |
| L6 | 2026-08-05 | All implementation happens in git worktrees; the primary checkout stays on `main` so any daemon-scheduled overnight run executes released code, not half-rewritten runner code. | Pipeline stability principle; daemon status checked at setup. |
| L7 | 2026-08-05 | "maddog" go signal received; Phase 4 CLAUDE.md hard-rule amendment ("board writes only the `tracking` and config tables") approved verbatim — will be applied in Phase 4 doc-sync. | User's departure message. |
| L8 | 2026-08-05 | Final acceptance gate added: real live run of `profiles/harish/` on the fully-merged `main-everything-db`; on failure → analyze (triager) → fix → re-verify until pass. Explicit owner override of the rajni-only default, for this final gate only. | User instruction in the maddog message. |
| L9 | 2026-08-05 | Phase 3 and Phase 4 specs MUST include existing-profile file→DB imports (dedup cache, registries, config documents) so harish's state carries over; without it the final live run would lose dedup state and create duplicates. | Advisor catch triggered by L8. |
| L10 | 2026-08-05 | Before ANY rajni verification run, confirm `profiles/rajni/profile.json` has `connector: "sqlite"` and the Notion mirror off (or restrict to non-sync stages / dry-run): the rajni fixture's profile.json carries the user's REAL Notion dbId. | Memory: local-db-adoption-design. |
| L11 | 2026-08-05 | `RunFolder.writeHeartbeat` has ZERO call sites today — heartbeat.json was never written by anyone. The DB heartbeat is wired for real: updated on every event-batch flush and at each stage start, making the 10-min staleness derivation meaningful. | Discovered during plan recon (grep). |
| L12 | 2026-08-05 | The run-store port carries `result`/`failure`/`syncDryrun` as OPAQUE `unknown` JSON blobs — `RunResult`'s schema stays in `ops/observability`, because `ports/` may import only `core/` (`ports-only-core`). Readers pass blobs through; the writer owns the shape. | Boundary rule would otherwise be violated. |
| L13 | 2026-08-05 | `SqliteRunStore` opens its DB LAZILY on first write and degrades to a warned no-op on open failure — so `wire()` (called by doctor) never creates/migrates a DB as a side effect, preserving `sqliteDbCheck`'s read-only posture and the fail-soft rule for notion-connector profiles. | Unconditional-DB (D5) reconciled with doctor's never-creates invariant. |
| L14 | 2026-08-05 | Task 13 (doc-sync incl. the pre-approved CLAUDE.md edits) is pulled OUT of the sdd-task-loop workflow and executed as a separate advisor-supervised dispatch: the workflow's protected-paths guard (CLAUDE.md, .claude/**) applies to all its agents, and relaxing it globally for one task would let fix-wave agents touch instruction files. | Advisor in-flight call after workflow launch failure. |
| L15 | 2026-08-05 | sdd-task-loop args MUST be a JSON object matching the script's schema ({worktree, briefsDir, tasks, gate, baseRef}), never a prose string — first launch failed on JSON.parse. Same lesson as the React-board package (args-as-string); now recorded here too. | Workflow failure ww28xlg9v. |
| L16 | 2026-08-06 | Hourly Telegram progress updates (user-ordered). The user asked for hourly Telegram progress updates during the overnight run. Advisor sends them via the bot token in `.env` to chat 8326684928 (harish's `settings.telegram.chatId`), scheduled via a session cron at :43 each hour, plus immediate pushes on milestones. Session-only — recreated if the Claude process restarts. | User request during overnight run. |
| L17 | 2026-08-06 | Phase 1 end-review outcome. Opus end review: needs-fixes — 3 important (db-path resolver split between run-store and board/migrate; `hasDb` semantics breaking the board's no_local_db branch once every profile has a DB; `runs.resumed_from` FK without ON DELETE aborting prunes that split a resume chain) + 6 minor. Fix wave `41c6f06` fixed all importants (gate 1389 green). Scoped re-review caught the `hasDb` fix stopping at the API boundary; fix round 2 `c70cbac` carried it through the UI (`pickProfile`/`ProfileSwitcher` now key off `connector`) and un-broke the tolerant `connector === ''` posture in the jobs gate (gates 1391 root + 115 UI green). | Boundary issue resolution. |
| L18 | 2026-08-06 | Deferred findings (advisor judgment). (a) Any `jobbunny.db` stamped user_version=2 by pre-fix commits on this branch keeps the old FK DDL — operational remedy: delete stale dev/fixture DBs before verification and note it in the PR; harmless on main (still v1). (b) Board `openStoreOrThrow` double directory-scan per request: negligible at local profile counts, deferred as an optional refactor. (c) Remaining review minors (failed-stage column in the runs list, per-method warn-once keying, structural never-throws heartbeat placement, resumedFrom recorded only on actual checkpoint resume, runs list total display) — shipped as-is or fixed in the fix wave; see review dossier in the PR. | Phase 1 review closure. |
| L19 | 2026-08-06 | Phase 2 design decisions (advisor, spec committed as `2026-08-06-checkpoints-db-phase2-design.md`). (a) Checkpoint group key is `(run_date, time_dir)`, NOT `run_id` — stage-run rows share a time_dir and that sharing IS the single-stage-chain hand-off; keying by run_id would silently break it. (b) Checkpoint writes are fail-LOUD (no lazy no-op fallback) — checkpoints are recovery data; parity with today's writeCheckpoint-throw-fails-the-run. (c) No file→DB checkpoint backfill (umbrella D7): checkpoints are same-day ephemera; a mid-day upgrade drops that day's resumability once. L9's import obligation stays targeted at Phase 3 (pipeline state) and Phase 4 (config). (d) CLAUDE.md's "Uniform checkpoints" bullet is NOT edited in Phase 2 — no pre-approved text exists and the standing rule requires explicit approval; the proposed replacement below ships in the PR body and the morning report for the user's decision, and lands as a follow-up once approved: "**Uniform checkpoints.** Each invocation owns its own checkpoint group (`run_date` + local `HH-MM` time-dir) in the per-profile sqlite DB (`checkpoints` table via `ports/checkpoint_store.ts`); the runner writes a checkpoint row after every stage. `--resume` seeds from the latest checkpoint in the latest earlier same-day group; `stage <name>` continues in today's latest existing group, so a chain of single-stage runs shares checkpoints. Checkpoint writes are fail-loud — losing one fails the run." | Advisor decision. |
| L20 | 2026-08-06 | Phase 2 execution outcome. 8-task sdd-task-loop green; Opus end review: 1 critical (daemon owed-slot evidence destroyed by folder retirement — a daemon restart would re-fire served slots) + 3 important + 6 minor → fix wave `e5fcd22` (daemon reads the `runs` table; group-shadow split `latestCheckpointTimeDir` vs `latestTimeDir`; cleanup prune wrapped). Advisor accepted two reviewer reversals of L19-era judgments: `written_by` is now POPULATED (provenance + runs↔checkpoints join) and `WITHOUT ROWID` was dropped (large payload rows). Fix round 2 `ca3d7da` (daemon per-read store + no-create read path, provenance wiring, schema/test hygiene, run_folder.ts→time.ts). Scoped re-review then caught a REAL blocking regression in the provenance wiring: the degraded run store's `startRun` sentinel `-1` passed the runner's `!== undefined` guard into the LOUD checkpoint write's FK → a degraded observability store would fail a healthy run; fixed in `b2bc769` (sentinel normalized to undefined in all three drivers; red-test reproduced the FK failure through the real stageCommand→SqliteCheckpointStore path). Doc-sync `d20a7c0` (explainer/triager/verify skill; CLAUDE.md deferred per L19d). Live verification on rajni (final HEAD): fresh v0→v3 migration; reconcile+filter+dedup+rank share one group with checkpoints at positions 0/6/7/8 and correct written_by; `run --resume` seeded from rank@8 and executed ONLY sync, landing in a `-2` collision-suffixed group (collision path exercised live); a killed mid-run process left an intact DB (integrity_check ok) and its orphan row flipped to crashed display after the 10-min staleness window; zero files under data/runs/; dedup's fail-loud missing-cache path captured in failure_json. | Advisor decision. |
| L21 | 2026-08-06 | Phase 3 design decisions (advisor, spec committed as `2026-08-06-pipeline-state-db-phase3-design.md`). (a) New generic `StateStore` port (`readDoc`/`writeDoc`, LOUD) with document keys = the legacy relPath strings verbatim; the fs `Storage` port SURVIVES for exactly two consumers — page-inventory reads (repo assets) and cleanup's legacy folder pruning. (b) The L9 import obligation is implemented as a LAZY one-time lift inside `SqliteStateStore.readDoc`: DB miss → read `<dataDir>/<key>` → insert → serve; legacy files are never deleted or modified (inert after lift). This supersedes umbrella D7 for these keys, uniformly across all ten (durable ones matter; lifting derived ones is harmless and keeps one rule). (c) `/structure` hand-off becomes two narrow CLI helpers (`jobbunny state read|write`, structure keys ONLY — not a generic DB tool); the markdown-table strings and 2500-char JD cap are transport-unchanged. (d) CLAUDE.md: the "Greenhouse/Keka company state is auto-managed in `data/registry/companies.json`" sentence becomes false; NOT edited in-phase — proposed replacement for user approval: "Greenhouse/Keka company state is auto-managed in the per-profile DB (`state_docs` key `registry/companies.json`); there are no hand-maintained board watchlists." | Advisor decision. |
| L22 | 2026-08-06 | Phase 3 execution outcome. | 8-task sdd-task-loop green (flagged tasks 3/5 passed immediate review); Opus end review: 2 important + 6 minor → fix wave `9f36e90` fixed the one real code bug (readDoc's lazy lift INSERTed a legacy file into `state_docs` BEFORE schema validation — a schema-drifted file would be cemented by the very read that throws; now validates first), fix round 2 `550bbef` (state write refuses TTY stdin; four stage doc comments follow the new transport), doc-sync `aa29e2a` (structure command → CLI helpers; verify skill's resume recipe → state_docs insert; explainer ports inventory fixed 9→12 closing a Phase 1+2 gap; triager already clean). Advisor rulings: `cache_gate.ts` extraction ACCEPTED (clean, reviewer-endorsed, recorded against the brief's "no new production files" line); cross-profile shared-sqlite-path state collision DEFERRED to Phase 4's config validation (extends a pre-existing jobs/tracking sharing surface, cheapest guard is a doctor/wire check where settings validation lives); `close()` not nulling `this.db` DEFERRED (established idiom across all three stores — change all or none, later). Live verification on rajni (final HEAD): fresh v0→v4; dedup's read LIFTED a real legacy cache file into `state_docs` leaving the file untouched; full chain + resume wrote ZERO files under data/ beyond jobbunny.db (no stray WAL, tracked fixtures untouched); markdown table round-tripped BYTE-EXACT through `state write`/`state read`; 1444 tests. |
| L23 | 2026-08-06 | Phase 4 design decisions (advisor, spec committed as `2026-08-06-config-db-phase4-design.md`). (a) The DB path becomes CANONICAL (`profiles/<name>/data/jobbunny.db`) and `settings.sqlite.path` is retired (wire loud-throws + doctor red with a move-your-file message) — verified safe: both real profiles carry `settings.sqlite: {}`. This dissolves the profile.json-in-DB bootstrap circularity AND L22's deferred cross-profile shared-path collision. (b) `config_docs` stores RAW document text (not re-serialized parses) — unknown-key and formatting fidelity preserved; zod validation sits at the WRITE boundary (per-key validators in core/config); each reader keeps its exact current posture (loud loader / tolerant board / safeParse-skip scan / raw setup). (c) The lazy lift validates JSON-validity only (NOT full schema) so legacy v0-key configs lift; and the store gains a READONLY lift mode so doctor/board/daemon reads never create a DB (doctor-never-creates preserved). (d) `resume.json` is stored as an opaque JSON doc; the dead `src/core/profile/` schema module (zero importers, shape mismatched with reality) is deleted. (e) `avoid.md` seeding deleted (dead surface). (f) The ONLY CLAUDE.md edit this phase is the L7 PRE-APPROVED hard-rule amendment (board writes tracking + config tables); all other stale CLAUDE.md lines (Profiles section file-editing language, avoid.md note, registry/companies.json sentence from L21) get proposed replacements in the morning report, not edits. | Advisor decision. |
| L24 | 2026-08-06 | **Phase 4 execution outcome.** 11-task sdd-task-loop green (flagged 4/5/9 passed immediate reviews; one harness hiccup — an agent completed its work but dropped its result envelope — recovered by cache-resume, zero rework). Opus end review: 2 critical + 4 important + 2 minor → fix wave `9dbf4cc` (legacy-file shadowing gets a `config-legacy-divergence` doctor check; doctor's config checks routed through the readonly ConfigStore; `profile build` no longer prints a false all-clear on a broken DB; Settings page treats absent docs as empty-and-editable) → fix round 2 `46fab5d` (runnable remediation string; doctor's wire-failure degrade now actually runs the five config checks; e2e no longer contaminates rajni's config rows — seed reset deletes config_docs, self-healing via re-lift; MalformedLegacyConfigFileError name contract). Doc-sync `9f97a95`: CLAUDE.md carries EXACTLY the pre-approved L7 hard-rule amendment (verified byte-exact, sole hunk); setup wizard steps 4/5/6/7/9 → `config get|set` pipes; explainer/verify/triager synced. Live verification (rajni, final HEAD): all four fixture docs lifted byte-exact; full chain + doctor ran green with every config file moved aside (DB-only); `config export` reproduced the tracked fixtures byte-exact; divergence check fired on a disk edit and cleared on restore; board PUT invalid→422/valid→200, POST create→201 seeding exactly three docs (resume.json deliberately unseeded — advisor override preserving setup's needs-action signal), traversal→404; `lane add-url` surgery landed in the doc; `profile remove --force` cleaned up. Gates: 1568 root tests, 127 UI vitest, 12/12 playwright e2e. Morning items: setup.md step 5 still says hand-editing resume.json is supported (misleading post-lift, one sentence); CLAUDE.md's remaining stale lines (Profiles file-editing language, avoid.md note, registry sentence per L21, `settings.sqlite` mention) have proposed replacements pending approval. | Advisor decision. |
| L25 | 2026-08-07 | **Final acceptance gate PASSED (live harish run, 2026-08-07 ~00:17 IST).** Run on feat/db-config HEAD `9f97a95` (content-identical to the pending PR #85 merge; user authorized running pre-merge while GitHub Actions was in major outage). Outcome **passed**, 28m27s: real LinkedIn harvest via the shared `.chrome-debug` session, funnel farm 0→1 / source 1→2 / all stages green / sync wrote local + pushed the real Notion mirror; jobs 64→66; Telegram digest delivered. DB migrated v1→v5 in place; all four config docs + all ten state keys lifted from the real legacy files (L9 dedup-state protection verified in production); 122 run_events + 10 provenance-linked checkpoints recorded; ZERO files written under data/runs/. Pre-migration backup at `profiles/harish/backup-pre-v5-2026-08-07/`. **Incident during the gate (user-caught):** the first attempt launched a LOGGED-OUT Chrome — `.chrome-debug/` resolves relative to the CODE CHECKOUT, so running worktree code with cwd at the primary repo spawned a fresh profile in the worktree. Run killed (its row shows crashed — honest), junk profile replaced with a symlink to the real `.chrome-debug`, re-run passed. NOT a product bug (checkout==cwd in normal deployment) but a sharp edge for anyone running worktree code against primary data — worth a KB note later. Also: the machine's daemon (old main code, pid 48085) was STOPPED before the gate — old code cannot open a v5 DB, so its next scheduled slot would have failed red; restart it after the user's merge decision puts compatible code on the running branch. PR #85 merge remains blocked on GitHub's Actions outage; the recovery cron merges when CI can run. | Final harish acceptance gate + incident + daemon restart notes. |
| L26 | 2026-08-07 | **Program close-out (2026-08-07).** PR #85 MERGED (`bf6b8ec`) after GitHub's ~9h Actions outage cleared — all four phases now on `main-everything-db`. The Windows CI leg caught genuine portability bugs pre-merge, two of them REAL production defects invisible on POSIX: `openJobsDb` leaked its handle when opening a non-database file (fixed: close-in-catch + regression test), and `wire()` never closed or exposed its `SqliteConfigStore` (fixed: `WireResult.configStore` + throw-path close). Fix rounds `8b60a36` + `8bfafb9` (wireMigrate's eager config-store handle in the test). Merged-tip re-confirmation: content-identical to the gate branch except this ledger; doctor green on harish (expected warns only: daemon deliberately stopped, Chrome idle); a real `reconcile` on merged code passed (cache rebuilt, 66 entries, runs row recorded, zero files). Program done-when satisfied end-to-end: schema v5, no per-profile JSON state/config writes, final live harish acceptance PASSED (L25). REMAINING USER DECISIONS: (1) merge `main-everything-db` → `main` (protected; not the advisor's call); (2) approve the deferred CLAUDE.md replacement texts (L19d checkpoints bullet, L21d registry sentence, L24 morning items incl. setup.md's stale resume-hand-edit sentence); (3) restart the daemon (`jobbunny serve start`) once compatible code is on the running branch — harish's DB is v5 now, old main code cannot open it; (4) delete `profiles/harish/backup-pre-v5-2026-08-07/` whenever comfortable. | Program closed-out; all four phases merged to main-everything-db; remaining user decisions noted. |
