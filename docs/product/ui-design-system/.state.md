# PM state — `ui-design-system`

Owner: product-pm. Purpose: let a re-dispatched instance resume without replaying grounding.
Last updated: 2026-08-25 · **Stage: COMPLETE. `spec.md` written; ready for product-ux.**

> **If you are a re-dispatched product-pm: the work is done.** `spec.md` exists and the interview is
> closed (one round, five questions, all answered — see §6). **Do not replay grounding.** Read
> `spec.md` first; this file is now only the audit trail behind it. `personas.md` was raised to
> **v1.4** by this run (JTBD-5 + two newly closed persona facts).

Target repo: `/Users/harishamutha/job-bunny-ui-design-system` (git worktree of Job Bunny, branch
`claude/ui-design-system`). **All reads and writes happen here, never in `/Users/harishamutha/Job-bunny`.**
Artifact dir: `/Users/harishamutha/job-bunny-ui-design-system/docs/product/ui-design-system/`.

---

## 1. The ask (verbatim, from the dispatch)

> "Build a UI design system for the Job Bunny board SPA and apply it across the whole UI, with the
> triage page's job-details section overhauled as its flagship application. Industry-standard for a
> mature SaaS product: (1) color scheme adapted from `docs/product/settings-overhaul/mockup.html`
> (closest to the Job Bunny logo); (2) a typography scale; (3) copywriting style and voice
> guidelines; (4) a vocabulary glossary with the product's reserved words — canonical names for
> stages, run states, lanes, profiles — one word per concept, used consistently in all UI copy.
> Tokens live as code (CSS variables/theme module), not just docs. Apply the system to every existing
> screen so the UI reads as one product, then rebuild the triage job-details section with it —
> information hierarchy, readability, and actions per the persona's triage workflow. UI-only: no
> pipeline or schema changes expected; any genuine backend need stays minimal and within the board's
> allowed write surface."

## 2. Frozen constraints (from SETUP — do not reopen)

- `ui/` is a Vite + React SPA under `ui/src` (`features/`, `components/`, `lib/`, `shared/`).
- `ui/` may import `src/core/**` only for **dependency-free** modules (today: `src/core/datetime/`,
  `src/core/normalize_token/`). A `core` module the SPA imports must import nothing itself.
- The board's write surface is a fixed allowlist (`ports/board.ts`). No pipeline or schema changes.
- File-size caps: 400 impl / 800 test, enforced over `ui/src/` and `ui/e2e/`.
- Tokens already exist: `ui/src/index.css` and `ui/src/lib/tokens.test.ts`. The design system must
  **build on / replace** these, never run in parallel.
- Decide as the persona: single Operator-Owner, three hats; triage = **job-seeker hat**.
- Simplest design that meets the need. No hypothetical flexibility.
- Docs-only guidelines (voice, glossary) must have an **enforcement/adoption path in code** where
  cheap, otherwise they rot.
- One spec = one shippable slice, with a stated preference: keep **design system + triage flagship**
  both Must in this slice, scoping "apply everywhere" as token/glossary **adoption**, not a redesign
  of every screen.
- UNATTENDED MODE: user is away; the orchestrator answers interview questions under written decision
  rules. Still run batched needs-input rounds, ≤3, every question with a recommended default.
- ONE researcher dispatch only — product-ux reuses this spec's research section.

## 3. Grounding status

| Item | Status |
|---|---|
| Persona (`docs/product/personas.md` v1.3) | **READ** — P1 "Operator-Owner", three hats, JTBD-1..4. Triage = job-seeker hat, JTBD-1. |
| Prior art: `settings-overhaul/spec.md` | **READ** — style, section shape, scorecard honesty bar. |
| Prior art: `settings-overhaul/ux-notes.md` §3 "Design Scale" | **READ** — an already-recon'd token table (colour, spacing, type, radii, component idioms). Strong early evidence for CHANGE-TO-EXISTING. |
| Prior art: `settings-overhaul/blueprint.md` §0 | **READ** — stack confirmed by direct read: React 19, Vite, Tailwind v4 (`@theme inline` in `ui/src/index.css`), shadcn style `radix-nova` + Radix meta-package, lucide, TanStack Query v5, hand-rolled hash router, sonner, Playwright e2e over the `rajni` fixture. Also records `--attention-strong` / `--success-strong` exist because plain `--attention`/`--amber` fail 4.5:1 as body text. |
| App recon (executor-fast) | **RETURNED** — see §4a. Follow-up dispatched for 5 gaps (tokens.test.ts contents, type-as-tokens, ui→core imports, `lane` rendering, casing consistency). |
| Mockup palette extraction (executor-fast) | **RETURNED** — see §4b. |
| Industry research (researcher) | DISPATCHED — one pass, both areas: design-token/colour/type/voice/glossary conventions **and** triage/detail-pane interaction patterns. |

## 4. Early classification hypothesis (to be confirmed by recon)

**CHANGE TO EXISTING**, not new-build. Evidence already in hand: `ui/src/index.css` carries a
`:root`/`.dark` token set plus `@theme inline` radius derivations; `ui/src/lib/tokens.test.ts`
already pins tokens; `ui/components.json` configures shadcn; a full component idiom table was
recon'd during `settings-overhaul`. **The stack question therefore does not arise** — it is
inherited and settled. The real ask is: formalise, fill the named gaps (type scale as tokens, voice,
glossary), enforce, and apply.

## 4a. Recon digest (executor-fast, returned)

**Stack (confirmed):** Tailwind v4 + CVA, no CSS-in-JS, no CSS modules. shadcn components checked
into `ui/src/components/ui/` (17 base components) over Radix headless primitives. React Hook Form,
sonner. Vitest + jsdom + Testing Library, colocated tests. **No Storybook / component explorer.**
281 `.ts`/`.tsx` files in `ui/src`. Largest impl files sit right at the cap: `DaemonCard.tsx` **400**,
`RolesCompaniesSection.tsx` 370, `Step6Launch.tsx` 368.

**Tokens that already exist** (`ui/src/index.css`):

| Group | Line | Note |
|---|---|---|
| Base colours (bg/fg/primary/secondary) | `:65-116` | light **and** dark variants |
| Semantic colours + `-strong` variants | `:81-97` | destructive / attention / success / amber |
| Sidebar colours | `:108-115` | |
| Chart series 1–5 | `:102-106` | |
| Font families | `:10-13` | `@theme` inline; Nunito Variable (heading) + Geist Variable (sans), via Fontsource |
| Radius scale sm–4xl | `:54-60` | base `1rem`, calc-derived 0.6×–2.6× |
| Motion `@utility hop` | `:177-182` | 150ms, respects `prefers-reduced-motion` |
| Mascot keyframes `lapin-hop` / `lapin-cheer` | `:194-212` | |

**The token gap is specific, not general:** colour, radius, font-family and motion are tokenised.
**Type SIZE / line-height / weight and spacing are not** — they are Tailwind default utilities applied
ad hoc per component. That is the single largest real hole behind ask-item (2).

**Screens (8):** triage · tracker · runs · analytics · settings/:section · job/:id · setup ("Operate")
· onboarding (wizard). "Apply everywhere" = these eight.

**Triage today** (`ui/src/features/triage/TriagePage.tsx:36`) — two panes:
- Left: undecided count badge (`:101`), debounced company search (`:111`, `/` focus, 250ms), Filter
  popover (`:104`), Date/Score sort toggles (`:119-136`), job rows, pagination (`:162-187`).
- Right, the job-details section under overhaul (`:191-217`):
  - `JobHeader.tsx:1-39` — title (h1), company (link to `url`), score badge, dateFound (relative,
    ISO in `title`), archived marker.
  - `JobFacts.tsx:1-53` — location (city + workType), timezone, seniority, skills (badges),
    matchReasons (bullets), reviewFlags (bullets), excitement badge.
  - `DecideBar.tsx:1-31` — **Apply (a)** / **Skip (x)** / **Save (s)** + current status badge.
  - `JdText.tsx` — full JD from `jd_json`.
  - `TrackingPanel.tsx:1-174` — 7 fields (status, dateApplied, compRange, contact, nextAction,
    nextActionDate, notes ≤5000), commit on blur/change, "Saving…" state.

**Board API job surface** — `GET /api/profiles/:name/jobs`, `GET .../jobs/:id`,
`PATCH .../jobs/:id/tracking`, `GET .../meta` (returns `statusOptions`, `excitementOptions`).
**Only write path for a job is the tracking PATCH.** Fields available but NOT displayed in triage:
**`lane`** (`linkedin` / `greenhouse` / `keka` — the job's source) and `id`. Everything else is shown.

**Vocabulary defects found in code (this is the evidence behind ask-item 4):**

| Defect | Evidence |
|---|---|
| **`Skip` (UI) = `Passed` (data)** | `DecideBar.tsx:23` label vs `DECIDE_STATUS.skip` → `Passed` (`src/core/tracking/vocab.ts:9-18`). Two words, one concept. |
| **`Save` (UI) = `Lead` (data)** | `DecideBar.tsx:26` → `Lead`. Worse: **"Save" collides with the form-save verb used across Settings**, so the same word means two different things in one product. |
| Run-state lexicon exists but only in one file | `ui/src/features/runs/runOutcome.ts:59-77` — "New jobs on your board", "Ran clean", "Ran with warnings", "Failed at `<stage>`", "Lost contact", "Telemetry missing", "Running — `<stage>` i/n". A de-facto glossary with no reuse path. |
| Stage names frozen | `runProgress.ts:4-15` — the 10 stage names are canonical already; nothing to decide, only to enforce. |
| Excitement vocabulary is idiosyncratic | `src/core/tracking/vocab.ts:31` — `Vera level`, `Kandipa podu`, `Try panalam` (Tamil). Deliberate personality or drift? Interview question. |
| Tracking statuses are canonical **and frozen** | `vocab.ts:9-18` — `Lead`, `Applied`, `Recruiter Screen`, `Tech Round`, `Onsite`, `Offer`, `Rejected`, `Passed`. CLAUDE.md: **Notion select option strings are byte-exact** — these cannot be renamed, only mirrored correctly in UI copy. |

## 4b. Mockup palette digest (executor-fast, returned)

`settings-overhaul/mockup.html` is **light-theme only** and single-hue: primary `#7b5ea7` (purple),
with `#ff8a3d` attention, `#c98a2e` amber, `#4caf6e` success, `#d64545` destructive. ~20 semantic
colours. System font stack in the mockup only (mockups must make zero external requests); the real
app uses Nunito/Geist via Fontsource.

**Finding that matters:** `run-experience-overhaul/mockup.html` uses a **drifted** version of the same
palette — `--muted-foreground` `#6d6280` vs `#6e5b87`; tints named `--primary-tint`/`--destructive-tint`/
`--amber-tint` there vs `--attention-10`/`--destructive-8`/`--success-10` in settings; borders as hex
in one and `rgba()` in the other. Two mockups, one product, three naming conventions. **This is
first-party evidence for the "reads as one product" problem, and it is drift in the design artefacts
themselves, not just the code.**

## 4c. Research digest (researcher — first pass PARTIAL, follow-up dispatched)

Returned: W3C Design Tokens (primitive → semantic → component tiers; intent-based semantic names);
Radix Colors 12-step scale (1–2 background, 3–5 component states, 6–8 borders, 11–12 text; APCA Lc 60 /
Lc 90 guarantees); Linear Triage (four actions on 1/2/3/H hotkeys, no multi-select); GitHub triage card
scannable in 3–5s; j/k navigation from vi/Gmail; soft-delete + undo window removes the need for
confirm dialogs.

**Explicitly NOT covered in pass 1 — follow-up dispatched to the same agent:** typography scales,
copywriting/voice guidelines (Polaris/Atlassian/Mailchimp/GOV.UK), **glossary rot-prevention
mechanisms (the most important gap)**, and job-posting detail hierarchy. The researcher reported no
public design-system material from ATS vendors (Greenhouse/Lever/Ashby) — likely a permanent
in-domain source gap to flag in the scorecard.

## 4d. Recon follow-up digest (executor-fast, returned) + PM first-hand spot-checks

**`ui/src/lib/tokens.test.ts` exists; there is NO `ui/src/lib/tokens.ts`.** It is a *CSS-text
assertion* test: it reads `ui/src/index.css` as a string and regex-extracts the `:root` and `.dark`
blocks, then pins **38 light + 38 dark colour tokens by literal hex**, the Nunito `@import`,
`--font-display` / `--font-heading`, `--radius: 1rem`, the `@utility hop` motion tokens, and the
`prefers-reduced-motion` guard. **Verified first-hand by PM** (`tokens.test.ts:18-136`).
**This is the repo's own, already-shipped enforcement idiom for a design token — zero new
dependencies, a plain Vitest file that reads source text. Every enforcement requirement in this spec
should copy it rather than invent a mechanism or add a linter dependency.**

**Typography is entirely untokenised.** `index.css:7-12` defines only `--font-display`,
`--font-heading`, `--font-sans`, `--font-mono` — **no size, line-height or weight metrics anywhere.**
Sizes are ad-hoc Tailwind defaults. Actual usage across `ui/src/**`:

| Utility | Occurrences |
|---|---|
| `text-sm` | 184 |
| `text-xs` | 75 |
| `text-lg` | 8 |
| `text-2xl` | 7 |
| `text-base` | 5 |
| `text-[10px]` (arbitrary) | 4 — `SettingsNav.tsx:106`, `SetupHealthCard.tsx:181`, `DiagnosisPanel.tsx:166`, `StageRail.tsx:115` |
| `text-[9px]` (arbitrary) | 1 — `FunnelTable.tsx:110` |

So there is a **de-facto 5-step scale with two off-scale escapes**, and 259 of 279 uses (93%) are the
two smallest steps. The scale is not missing so much as *unnamed and unbounded*.

**`ui/` → `src/core/**` seam — narrower than feared:** only `src/core/datetime/index.ts` (9 call
sites) and `src/core/normalize_token/index.ts` (1 site, `whereYouWork.model.ts:13`).
**`src/core/tracking/vocab.ts` contains only `export const` / `export type` and imports nothing — it
is dependency-free and therefore UI-importable.** This is the single most important feasibility fact
for the glossary requirement, and it is still marked PROVISIONAL until product-ui confirms.

**`lane` is rendered nowhere on a job.** Its only appearance is a settings checkbox label rendering
the raw identifier (`WhereJobsComeFromSection.tsx:233` → "linkedin" / "greenhouse" / "keka"),
which is itself a vocabulary defect: raw internal identifiers used as UI labels.

**Copy casing is consistent** — uniformly sentence case across 5+ feature folders sampled. **Honest
consequence: the voice pillar has a much lower defect load than the ask assumes, and the spec should
say so rather than manufacture work.** The real copy defects are vocabulary collisions, not casing.
Config filenames leak user-visibly in `RawConfigSection.tsx:223` (tab labels — defensible, it *is*
the raw editor) and `RolesCompaniesSection.tsx:213,258,321` (help text — deliberate, inherited from
`settings-overhaul`).

### PM first-hand spot-checks of the triage flagship (read directly, not delegated)

| Defect | Evidence |
|---|---|
| The detail pane hand-rolls a card instead of using `Card` | `JobFacts.tsx:10` — `rounded-lg border border-border` vs the shipped card idiom `rounded-xl` + `ring-1 ring-foreground/10`, no border |
| Positive and cautionary signals render identically | `JobFacts.tsx:25-44` — "Why it matches" and "Review flags" are the same bullets in the same `text-muted-foreground`. Opposite meanings, identical weight. |
| Location / timezone / seniority collapse into one undifferentiated muted line | `JobFacts.tsx:11-15` |
| The score — the most decision-relevant number in triage — is an unlabelled `Badge` with no scale context | `JobHeader.tsx:28`, `JobRow.tsx:48-52` — renders e.g. `74` with no "out of" and no label |
| The JD is an unbounded `<pre>` with no measure cap and no progressive disclosure | `JdText.tsx:10` — `whitespace-pre-wrap ... leading-relaxed`, full pane width. Research says 45–90 chars/line. |
| Raw unicode glyphs where the rest of the product uses lucide | `DecideBar.tsx:20,23,26` (`✓ ✗ ☆`); `TriagePage.tsx:126,135` (`↑ ↓`) |
| A reversible action is styled destructive | `DecideBar.tsx:22` — `variant="destructive"` on Skip, though the component's own doc comment says decisions stay changeable |
| Job status is encoded by **hue alone** | `JobRow.tsx:5-10,42-46` — a 6px `rounded-full` dot; the only non-colour cue is a `title` attribute. **Directly contradicts the shipped run-experience principle "state = visual weight, not hue, never colour alone"** (`run-experience-overhaul/ux-notes.md:33-36`). This is the sharpest single piece of "does not read as one product" evidence. |
| The Skip/Save mismatch is *documented as deliberate* | `decide.ts:3-13` — the doc comment says "spec decision, not the vocab's own authority". Reversing it is a real decision, not a bug fix. |

**Scope fact that matters:** `JobHeader` and `JobFacts` live in `ui/src/features/job/` and are
**shared by the triage detail pane and the standalone `#/job/:id` page** (`JobHeader.tsx:9` says so).
Rebuilding the triage job-details section therefore changes the job page too — for free, but it must
be stated, and `#/job/:id` must be in the acceptance criteria.

## 4e. Research digest (complete after follow-up)

**Tokens.** W3C Design Tokens (v1, Oct 2025): three tiers, primitive → semantic → component;
semantic names must be intent-based ("text/subdued", not "text/gray").
Radix Colors: 12-step scale (1–2 background, 3–5 component states, 6–8 borders, 11–12 text) with
APCA Lc 60 / Lc 90 guarantees. Tailwind v4 ships tokens as CSS custom properties via `@theme`.

**Type scale.** Real ratios in shipped systems: Polaris **1.2**, Atlassian **1.2** (base 16px, body
14px/20px, everything on a 4px grid), Material 3 **15 styles** in 5 categories × 3 sizes, Carbon a
formula (`y₀ = 12px`), Tailwind default ~1.125–1.25. Butterick: **45–90 characters per line**.
`font-variant-numeric: tabular-nums` is the standard recommendation for data-dense UIs (Ant Design
PR #11567). Carbon/Material handle table density via density *modes*, not raw line-height values.

**Voice.** Polaris: sentence case, contractions encouraged, imperative verb + object on buttons, one
verb per button, never "Submit". Atlassian: brief, direct, action-oriented, plain language. Mailchimp:
active voice mandatory, plainspoken, lead with the main point. GOV.UK: short words ("buy" not
"purchase"), an A–Z of banned terms, no "please". **Convergent minimum set (3–6 rules): short plain
words · active voice + imperative verbs · sentence case · lead with the main point · no jargon ·
one reserved term per concept.**

**Glossary rot.** GOV.UK A–Z is the reference structure (100+ "don't say" terms with plain-English
replacements). Enforcement options found: **Vale** (prose linter, style packs, CI, `.vale.ini` — the
lowest-setup dedicated tool), textlint, alex, and **`eslint-plugin-i18next`'s `no-literal-string`**
(flags hardcoded UI strings, forcing them through a key catalogue). Published finding: **static
glossaries decay fastest without enforcement; living documentation generated from code plus a CI
block is what prevents rot.** Evidence that terminology drift causes real harm: calling one component
"button"/"CTA"/"action" confuses teams and undermines trust in the system (UXPin/NN-g).
*PM note: none of these tools should be adopted here — `tokens.test.ts` already proves a
zero-dependency Vitest source-scan does the same job at this scale.*

**Triage patterns.** Linear Triage: exactly four actions on hotkeys (1/2/3/H), no multi-select.
GitHub triage cards are designed to be scannable in **3–5 seconds**. j/k navigation from vi/Gmail.
Soft-delete plus an undo window removes the need for confirmation dialogs.

**Job-posting hierarchy.** Expected order: title → location → employment type → short summary →
2–4 responsibilities → team context → benefits. Seekers spend **~14.6s on requirements** and **~1
minute on a whole posting**; the initial scan is **6–7 seconds** before deciding to read on.
**Salary is the dominant abandonment trigger: 60% won't apply without it (Monster); postings with a
range get 36% more applications; 91% of new Indeed postings now include one.**

**In-domain source gap, to be flagged in the scorecard:** no ATS vendor (Greenhouse / Lever / Ashby)
publishes a design system, and no job-board usability study was found. The triage hierarchy
recommendations are transferred from generic review-queue patterns plus recruiting-side job-ad
guidance, not from validated seeker-triage research.

## 5. Interview

**CLOSED — one round, five questions, all answered on the recommended default.** No round 2 was
needed: nothing remaining would have changed the spec, and proportionality says don't spend a round
to confirm what the evidence already decides. Full resolutions are in `spec.md` §8. Summary:

| # | Answer |
|---|---|
| 1 | (b) keep dark tokens, no toggle — **AMENDED**: dark mode **is** reachable (OS-following, `main.tsx:8-11`). Both modes live; AA in both is an acceptance criterion. |
| 2 | (c) vocabulary module + Vitest source-scan, zero new deps, small precise banned list. |
| 3 | (b) restyle/re-rank, surface `lane`, disclosed JD, labelled score, split reasons/flags, non-colour-only status, lucide. Scraped comp → Out of Scope. |
| 4 | (b) canonical reference doc, machine-checked. No in-app gallery. |
| 5 | 5a yes (Pass / Lead) · 5b yes (~6-rule voice set) · 5c keep the excitement vocabulary as reserved. |

## 6. Decisions taken (final)

- **Classification: CHANGE TO EXISTING.** Not greenfield; the stack question does not arise.
- **One slice, not two** — system + triage flagship both Must, with "apply everywhere" scoped to
  **token / type-scale / glossary adoption**, explicitly NOT a redesign of the other seven screens.
- Tracking status strings, the 10 stage names, and the excitement levels are **frozen** (Notion
  byte-exactness + architectural contract). The glossary mirrors them; it never renames them.
- **Verdict: BUILD, reshaped.** The reshape reallocates effort: colour is documentation not
  construction, voice is a short rule set not a copy campaign, type scale and glossary are the real
  holes, and **the triage rebuild is the majority of the value, not the finale.** If the epic must
  shrink it shrinks by dropping system polish, never triage. Reduced-scope fallback if the n=1
  objection is judged decisive: **triage + the vocabulary module only** (R11–R12, R17–R24).
- 33 requirements (R1–R33): 20 Must, 5 Should, 1 Could, 7 Won't. 15 acceptance criteria, one per
  pillar plus regression guards.

## 7. Artifacts written by this run

| Path | Note |
|---|---|
| `docs/product/ui-design-system/spec.md` | The deliverable. |
| `docs/product/ui-design-system/.state.md` | This file. |
| `docs/product/personas.md` | **v1.3 → v1.4.** Added JTBD-5 (job-seeker hat, the triage decide loop) and two newly closed persona facts: dark mode is OS-following and therefore live; the excitement vocabulary is reserved. |

## 8. Handover notes for product-ux

- **Reuse `spec.md` §3 — it is the pipeline's only research pass.** Do not commission a second.
- **The mockup must show dark mode.** It is a live surface (`main.tsx:8-11`) that no prior mockup has
  ever covered, and G8 is one of the four things that survived the case against.
- **Do not re-recon tokens from `index.css` and re-type them into a new Design Scale table.** That is
  exactly how the two existing mockups drifted (§1). Cite R4's reference document.
- Six named-open usability concerns are in the §15 Usability cell, (a)–(f). (a) is the flagship's
  central visual problem: distinguishing match reasons from review flags **without** hue, since R15
  forbids the obvious green/red answer.
- Treat §10's field ordering as a **hypothesis, not a finding** — scorecard weakness 4.
