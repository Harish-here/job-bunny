# PM state — `settings-overhaul`

Owner: product-pm. Purpose: let a re-dispatched instance resume without replaying grounding.
Last updated: 2026-08-17 · **Stage: COMPLETE. `spec.md` written; ready for product-ux.**

> **If you are a re-dispatched product-pm:** the work is done. `spec.md` exists and the interview is
> closed (one round, eleven questions, all answered — see §6). Do not replay grounding. Read
> `spec.md` first; this file is now only the audit trail behind it.

---

## 1. The ask (verbatim, from the dispatch brief)

> The board's Settings page is a pre-UI-era handover: its information architecture is the CLI era's
> config-file layout (one tab per doc: `profile.json`, `filter.json`, `resume.json`,
> `search_urls.md`), with raw-JSON escape hatches carrying everything the forms don't render and CLI
> commands printed as hint text. Author the product spec for a full overhaul that reorganizes the
> settings/control surface around user intents — tune matching, pace scraping, operate the machine,
> connect integrations, get set up — not around which file the answer lives in.

Four drivers, ALL Must-level, one spec (charter one-slice rule explicitly overridden by the brief):
1. Surface missing config (JSON-hatch-only knobs).
2. Organization & usability (intent-oriented restructure).
3. Ops control center (daemon/schedule, run cadence, Chrome/login health, breaker state).
4. Onboarding/first-run (secrets, resume, search URLs; less CLI dependence).

Brief: `/Users/harishamutha/Job-bunny/docs/product/settings-overhaul/pm-brief.md` (frozen sections:
"Closed decisions", "Hard constraints").

## 2. Frozen decisions (do not reopen)

- Mega-epic slicing: all four drivers in one spec; phasing inside it, NOT MoSCoW demotion.
- Hub-vs-Settings boundary: deliberately OPEN. PM frames the question; product-ux proposes; user
  rules at the mockup gate. PM must not answer it.
- Board binds 127.0.0.1, never spawns runs (inserts `run_intents`; daemon is sole spawner).
- Daemon lifecycle control from UI is architecturally unresolved — state the requirement, defer the
  mechanism to the BE-blueprint gate.
- Chrome/LinkedIn login is a PARKED separate epic (`setup-overhaul`, `jobbunny login`). Treat as a
  dependency interface only.
- Breaker live state is NOT board-visible: session-scoped, in-process, lane constants. Only a PAST
  trip is inferable via substring match on warn text (`src/app/features/runs/soft_errors.ts:42-49`).
- Cross-platform; autostart darwin-only.

## 3. Grounding done

- Recon (settings surface): `docs/product/settings-overhaul/recon-settings-surface.md` — READ. Six
  tabs, per-tab JSON hatch, gap list §3, write contract §2. Uncertainty boundary declared in its own
  NOTES.
- Persona: `docs/product/personas.md` v1.2 — READ. P1 "Operator-Owner", single user, two hats
  (job-seeker / operator-under-duress). Telegram-first failure discovery; board is drill-down.
- Prior art (must not contradict): `docs/product/run-experience-overhaul/spec.md` and `ux-notes.md`
  — READ. Key inherited decisions: sidebar shell (triage/tracker/runs/analytics/setup/settings),
  Run Now block with persistent last-run line, daemon health probe at queue time (R5), diagnosis
  panel classes incl. (ii) breaker open and (iii) daemon down with `[Copy: jobbunny serve start]`,
  visual idiom tokens, "urgency = weight not hue", zero-text-input design, no charting dependency
  pre-approved.
- Industry research: ONE dispatch to `researcher` (product + interaction patterns together) —
  status recorded in §5.
- Targeted recon commissioned (authorized by the brief, outside the settings-surface boundary):
  (a) config knobs/defaults/validators; (b) ops surface — hub page, daemon, run intents, doctor
  checks, breaker internals, Chrome login health, board route inventory.

## 4. Decisions taken so far (PM judgment)

- Classification: CHANGE TO EXISTING. Evidence: `ui/src/features/settings/SettingsPage.tsx` renders
  six working tabs today; stack settled and inherited from `run-experience-overhaul` (React 19,
  Vite, Tailwind v4, shadcn/Radix, TanStack Query, hash router). No stack question in the interview.
- Persona needs no re-derivation; refine `personas.md` at the end of this run if the interview
  yields anything new (candidate: whether the user ever creates a NEW profile — bounds driver 4).

## 5. Findings from delegated work

### researcher — DONE. 35-row sourced table. Load-bearing citations for the spec:

- Settings IA must match the user's mental model, established by card sorting, not the underlying
  file structure — NN/g Information Architecture, https://www.nngroup.com/topic/information-architecture/
- Sidebar (not tabs) is the pattern when there are many sections plus hierarchy; fix the nav, scroll
  the content — https://www.alfdesigngroup.com/post/improve-your-sidebar-design-for-web-apps (2026)
- Progressive disclosure: hide advanced controls until relevant; tiered novice/expert reveal —
  https://lollypop.design/blog/2025/may/progressive-disclosure/ (2025)
- Validate on submit, NOT as-you-type; show an error summary at the top AND inline per field; do not
  disable the submit button (users read disabled as system status) —
  https://design-system.service.gov.uk/components/error-message and /patterns/forms
- Raw-editor duality is a legitimate shipped pattern: VS Code's `workbench.settings.editor` toggles
  ui|json over ONE source of truth — https://code.visualstudio.com/docs/languages/json
- Grafana's asymmetry: once provisioned from YAML, the UI goes read-only rather than fighting the
  file — https://grafana.com/docs/grafana/latest/administration/provisioning/
- Scheduler/daemon consoles surface last run + next run + per-task manual trigger: Sonarr/Radarr
  System→Tasks (https://wiki.servarr.com/radarr/system), Oracle Scheduler dashboard, Dagster Daemons
  tab. Radarr's "manual trigger icon at the far right of each scheduled task row" is the closest
  precedent for run-now-per-schedule.
- Circuit-breaker state is conventionally surfaced as Closed/Open/Half-Open with transition history;
  "bouncing between Open and Half-Open" reads differently from "Closed for weeks" —
  https://grafana.com/grafana/dashboards/21307-circuit-breaker/ and
  https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker
- Scraper pacing: jitter + exponential backoff, and the client should pace itself BEFORE the server
  pushes back — https://www.zenrows.com/blog/429-too-many-requests, https://scrappey.com/qa/crawling/what-is-throttling
- Secrets: never return the value from the API; show configured/not-configured —
  https://docs.apify.com/actors/development/actor-definition/input-schema/secret-input (matches the
  board's existing presence-only `GET /api/secrets`, so this is validated, not new)
- Destructive-action friction should MATCH blast radius: no confirm for reversible, type-to-confirm
  for permanent — https://www.saasui.design/blog/saas-destructive-actions-confirmation-ux-patterns (2026)
- Settings search becomes worth it past ~24 settings — https://bricxlabs.com/blogs/settings-page-ui-examples
- Reset-to-default should be visually subdued and name the exact consequence —
  https://www.designmonks.co/blog/reset-button-ui
- Material: call it "Settings"; prioritise frequently-used; keep the count manageable —
  https://m1.material.io/patterns/settings.html
- Polaris app-settings layout pattern — https://polaris-react.shopify.com/patterns/app-settings-layout
- Researcher's own declared gaps: no published usability research on prosumer personal-automation
  tools; no direct source for "every preference is a failure"; no design-system guidance on
  rate-limit UI in scrapers (structural transfer from observability literature only). Record this
  thinness in the scorecard — the pacing-UI and breaker-UI patterns are transferred, not sourced
  in-domain.

### executor-fast (config knobs) — DONE. The gap list is MUCH bigger than the brief's §3 implies.

- LinkedIn pacing, `src/cli/wire/settings.ts`: jitterMinMs 5000 (:88), jitterMaxMs 12000 (:89),
  interUrlDelayMinMs 20000 (:95), interUrlDelayMaxMs 45000 (:96) — fail-loud if min > max.
  Also maxCardsPerUrl 40 (:66), maxAgeDays 30 (:25).
- Budget/registry knobs, same file: reprobeNotFoundAfterDays 30 (:46), maxProbeFailures 3 (:47),
  staleAfterFetchFailures 3 (:48), maxProbesPerRun 25 (:53), maxNewPerLane 40 (:59). **None of
  these appear in the brief's gap list** — `maxNewPerLane` and `maxProbesPerRun` directly bound how
  many jobs a run can produce, which is the persona's headline number.
- **RANKING is entirely unsurfaced** (`src/core/rank/rank.ts`): skills.primary (:69),
  primarySkillPoints 1.0 (:75), secondarySkillPoints 0.5 (:78), skills.maxPoints 40 (:80),
  denomMin/Max 3/8 (:84-85), title.domainKeywords (:105), title.maxPoints 15 (:107),
  title.neutralPoints 8 (:110), seniority.targets (:117) + maxPoints 15 (:119),
  location.homeCities (:125), acceptableTimezones (:127), borderlineTimezones (:129),
  bonus/partial 20/10 (:131-133), workTypePreference multipliers (:164), softVerdictPenalty 5 (:171).
  This is the single largest omission: ranking decides the ORDER of the board, and none of it is
  editable outside raw JSON. Not in the brief's driver-1 list.
- Cleanup (`src/routines/cleanup/cleanup.ts`): passedOlderThanDays 7 (:43), untouchedOlderThanDays
  30 (:45), runsOlderThanDays 30 (:49), checkpointsOlderThanDays 2 (:53).
- Notion (`src/adapters/db/notion/connector.ts`): mirror default false (:33), dryRun default true (:29).
- Telegram (`src/adapters/notify/telegram/telegram.ts:21`): chatId required, fail-loud at wire.
  Nothing else about digest shape is configurable — the digest is code, not config. **This corrects
  the brief's "Telegram digest" gap item: there is no digest-shape config to surface.**
- Logging fileLevel / ttyLevel, env-overridable.
- `PipelineConfig.settings` is `Record<string, unknown>` — adapter-specific settings (`cdp-chrome`,
  `claude-cli`) are validated only by their own constructors at wire time, so a UI form over
  `settings` cannot rely on one central schema.
- Writes go to the `config_docs` table, validated strictly by `validateConfigDoc` (unknown top-level
  keys rejected); reads never parse. Changes take effect on the NEXT RUN — `wire()` re-reads config
  each invocation and the daemon re-checks per tick. **No restart needed; no live-run effect.** This
  is a genuinely good property to surface in the UI ("takes effect from your next run").
- CONTRADICTION FLAGGED: the recon claims `filter.json`'s `companies` and `timezones` are parsed but
  never read (dead config), which contradicts CLAUDE.md ("use `filter.json`'s `title`/`companies`
  blocks instead" of `avoid.md`). Verification dispatched — see §5c. **Do not write a requirement to
  surface a control that does nothing.**

### executor-fast (ops surface) — DONE.

- **Hub already IS a six-card setup-and-health page** (`ui/src/features/hub/hub.model.ts:41-71`):
  Profile (connector, lanes, notifiers, routines) · Persona & filters (skills, seniority, location)
  · Search URLs · Integrations (Notion mirror, Telegram tokens) · Schedule & daemon · Pipeline
  health (Chrome, Claude CLI, local DB, remaining doctor checks). Cards are populated by mapping
  doctor checks to cards (`CHECK_TO_CARD`, :85, fallback to pipeline-health). **Every one of those
  six cards names a topic Settings also owns.** The overlap is total, not marginal — this is what
  makes the Hub-boundary question the highest-stakes open item in the epic.
- Daemon: pidfile `<root>/.jobbunny-daemon.pid` (`src/ops/daemon/pidfile.ts:10-13`); tick 30s
  (`src/ops/daemon/daemon.ts:62`); `GET /api/daemon` returns `{state, pid, startedAt, lastTickAt,
  inFlight, profiles[]}` (`src/app/features/daemon/routes.ts:19`). Autostart writes
  `~/Library/LaunchAgents/com.jobbunny.autostart.plist`, `RunAtLoad true`, no calendar interval,
  darwin only (`src/cli/commands/autostart.ts:30,64`).
- Run intents: `POST/DELETE/GET /api/profiles/:name/run-intents`
  (`src/app/features/intents/routes.ts:116-129`, list limit fixed at 20). Row shape `{id,
  requestedAt, status: pending|claimed|cancelled|expired, claimedRunId}` (`src/ports/run_intents.ts:6-11`).
  Daemon claims via `claimIntent` (`src/cli/wire/daemon.ts:231-240`), swallowing failures.
- Doctor: 8 core checks — profile-parses, filter-parses, empty-lanes, sqlite-path-retired,
  config-legacy-divergence, env-tokens, claude-cli-on-path, daemon-liveness
  (`src/ops/doctor/aggregate.ts:316-327`) — plus conditional adapter checks:
  linkedin-inventory-freshness, cdp-reachable, notion-db-reachable, sqlite-db-openable,
  telegram-bot-token (`src/cli/wire/builders.ts:349-387`). 13-14 total. Result shape
  `{check, status, detail}`, overall = worst finding (`src/ports/doctor.ts:19-22`).
- **No LinkedIn-login health check exists.** `cdp-reachable` probes the CDP port only — it proves
  Chrome is up, not that the session is signed in. Confirms the parked `setup-overhaul` gap.
- **BREAKER — CONTRADICTS A FROZEN CONSTRAINT IN THE BRIEF.** The brief states the breaker's live
  state is "session-scoped, in-process, held in lane constants" and NOT board-visible. Evidence says
  otherwise: there is a **breaker store persisted to disk** at
  `<userDataDir>/.jobbunny-linkedin-breaker.json` (`src/adapters/lanes/linkedin/breaker_store.ts:6`),
  scoped to the shared `.chrome-debug` profile and therefore cross-profile, not per-profile (:8-10).
  Constants: trips after **3** consecutive shell outcomes
  (`throttle.ts:24`, `THROTTLE_CONSECUTIVE_SHELLS_TO_TRIP`), cooldown **4 hours** (`throttle.ts:29`).
  A file at a known path is readable by the board process. So live breaker state is very likely
  surfaceable — subject to the board resolving `userDataDir`, which is a real open question, not a
  formality. **PM must NOT silently override a frozen constraint** — raised to the user as Q5.
  Past-trip inference via three exact warn substrings is confirmed
  (`src/app/features/runs/soft_errors.ts:46-50`).
- Board write surface confirmed exactly as CLAUDE.md states: read-only except `PATCH /tracking`,
  `PUT /config/:doc`, `POST/DELETE /profiles`, `PUT /secrets/:key`, `POST/DELETE /run-intents`.
  Also present: `GET /api/app`, `GET /api/personas`, `GET /runs/:id/soft-errors`,
  `GET /runs/deferred-slots`.

### 5c. Verification — DONE. The "dead config" claim was FALSE. Corrected picture:

- `filter.json` top-level keys: **title, companies, locations, timezones, skills**
  (`src/core/filter/config.ts:16-47`).
- `companies` IS read at runtime — `src/core/filter/rules/company.ts:9,11` (`companies.avoid.some()`).
- `timezones` IS read at runtime — `src/core/filter/rules/timezone.ts:13,16,20`, gating remote roles.
- The board edits only `title`/`locations`/`skills`; `companies` and `timezones` are deliberate
  pass-throughs (`ui/src/features/settings/sections/filters.model.ts:62-64,105-108`). Pass-through
  is NOT dead — surfacing them is legitimate and the brief's driver-1 item stands.
- **Ranking config lives in a DIFFERENT DOCUMENT**: `profile.json` under `settings.rank`, parsed at
  `src/cli/wire/compose.ts:362` (`RankConfigSchema.parse(config.settings.rank ?? {})`). The board
  never touches `profile.json.settings` at all.
- **The single best piece of evidence for the whole epic's thesis:** "where I'm willing to work" is
  split across TWO documents with TWO different semantics — `filter.json.timezones` decides
  pass/fail (a job is dropped), while `profile.json.settings.rank.location.acceptableTimezones` /
  `.borderlineTimezones` decide scoring (a job ranks higher or lower). Zero overlap, zero
  cross-reference in the UI. One user intent, two files, no surface expresses it. A file-shaped IA
  structurally CANNOT express this; an intent-shaped one must. Cite this in Problem & Goal.
- Correction to the earlier knobs recon: the confusion arose from conflating the two timezone
  configs. Nothing in `filter.json` is dead.

## 5b. PM spot-checks (Read, verified first-hand)

- `ui/src/features/settings/SettingsPage.tsx:12-19,84` — six tabs confirmed; the JSON hatch renders
  for EVERY doc-backed section (`{doc && <JsonEscapeHatch …/>}`). Dead `PLACEHOLDER_COPY` map at
  :30-34 still names resume/search-urls/danger as "coming soon" though all three are implemented —
  minor stale code, note in spec NOTES.
- `ui/src/features/settings/sections/ProfileSection.tsx:17` — comment: "`settings` is deliberately
  not form-edited here". :86-97 — connector is a bare `<select>` flipping sqlite↔notion, i.e. the
  source-of-truth switch, with NO confirm, NO warning, NO consequence copy. Highest-blast-radius
  control on the page. :149-152 — the exact seam copy: "Notion and Telegram settings are configured
  from Setup & Health → Integrations, or through Edit as JSON."
- `ui/src/features/settings/sections/ScheduleSection.tsx` — daemon status is already richer than the
  recon implies: six rendered states (loading / api-unreachable / degraded-with-schema-vs-build /
  stopped / stale-"Wedged" / running), each with last-tick seconds, plus "Next run (saved)". Two CLI
  strings are printed as remediation: `jobbunny serve stop && jobbunny serve start` (:22, degraded)
  and `jobbunny serve start` (:194, stopped). Save is EXPLICIT (a Save button), not autosave; there
  is no dirty-state guard. It imports `../../wizard/wizard.queries` — a `wizard` feature already
  exists in `ui/src/features/`, relevant to driver 4.

## 6. Interview log

**Round 1 — returned to the orchestrator, awaiting answers.** Ten questions, each with a
recommended default:

1. Which frictions are real in the last month (evidence for requirements) — open.
2. Who is onboarding for (bounds driver 4's value). Default: completeness/repair checklist first,
   new-profile creation second, never reimplement the Claude-dependent parts.
3. Ops control depth. Default: control what the live daemon can do (pause schedule, skip next run),
   AND state full start/stop as a Must with the mechanism deferred to the BE gate.
4. Pacing knobs. Default: presets + advanced disclosure with a consequence warning.
5. Raw-JSON hatch fate. Default: one consolidated advanced/raw area, removed from per-intent
   sections.
6. Global (machine) vs per-profile split. Default: make it explicit; note it feeds the Hub question.
7. Connector switch + Notion mirror. Default: connector becomes read-only in the UI; expose mirror
   on/off + dryRun instead.
8. Merge resume fields into a "how jobs are matched" area. Default: yes.
9. Rule-change preview ("this would have dropped 189 of 214"). Default: cheap static preview against
   the last run if feasible, else none. Flagged as highest-value / most likely to be cut.
10. Phase order. Default: restructure IA first, then surface missing config into it, then ops, then
    onboarding.

**Round 1 — ANSWERED. Round 2 not needed; no blocking question remained.** Resolutions:

| Q | Answer |
|---|---|
| 1 | Real pains: **P-a** raw-JSON editing · **P-b** daemon/login blindness · **P-c** thin run, cap-or-rule suspected. NOT selected: pacing-after-soft-block, new-profile setup. |
| 2 | (b) retunable rank lists only; point weights stay raw. |
| 3 | (c) completeness/repair first, new-profile later; Claude-dependent steps never reimplemented. NOT (d) — not for other people. |
| 4 | (b) + rider: live-daemon control now; full start/stop a Must with mechanism deferred to BE gate. |
| 5 | (a) live breaker state board-readable; **no manual reset**. Brief's constraint formally superseded (orchestrator independently verified `breaker_store.ts`). |
| 6 | Split by risk: pacing → presets + advanced disclosure + consequence warning; budget caps → raw fields with bounds. |
| 7 | (b) one consolidated Advanced/raw area. |
| 8 | (a) explicit machine/profile split, without pre-deciding the host page. |
| 9 | (b) connector read-only; expose mirror + dryRun. |
| 10 | (b) cheap static preview vs. last run's jobs; feasibility flagged for BE; cut cleanly if infeasible. |
| 11 | (d) delegated to PM — derived in spec §12. |

## 7. Deliverable + downstream handoffs

**`/Users/harishamutha/Job-bunny/docs/product/settings-overhaul/spec.md`** — all four drivers,
Phase Decomposition (§12) after Requirements (§11), four per-driver sub-verdicts (§7.3), Hub
boundary framed-not-answered (§7.5), every hard constraint reflected.

Verdicts: driver 1 **BUILD reshaped** · driver 2 **BUILD** · driver 3 **BUILD reshaped** ·
driver 4 **RESHAPE** (with new-profile creation called **not needed** in this spec).

Phases: 1 intent IA + thin-run knobs · 2 ops read-only · 3 ops control (write-surface expansion,
BE-gated) · 4 setup completeness · tail = cheap extras.

**Open items deliberately left for later stages:**
- Hub-vs-Settings boundary → product-ux proposes (4 options framed in §7.5; C+D compose, flagged as
  an observation not a decision), user rules at the mockup gate.
- R20 daemon lifecycle mechanism → BE-blueprint gate, user decides.
- R15 preview feasibility → BE spike; must reuse `core/filter` + `core/rank` directly or not ship.
- R17 LinkedIn login signal → does not exist at any layer; new backend work, must not launch Chrome
  on a page load.
- R18 → board must resolve `userDataDir` (a Chrome-profile path it does not currently know).

**`personas.md` updated to v1.3:** third hat ("tuner"), JTBD-4, Q1 friction evidence, and one
question CLOSED — the audience is one person permanently, so onboarding-for-strangers is not a real
requirement in this product.

---

# UX state — `settings-overhaul` (product-ux, appended 2026-08-17)

Owner: product-ux. **Stage: COMPLETE** — `ux-notes.md` + `mockup.html` written. No interview round
was needed; no question was blocking. The one open item is a USER RULING at the mockup gate, framed
below.

## UX.1 The ask (verbatim, from the orchestrator dispatch)

> Epic slug: settings-overhaul. Target repo root: /Users/harishamutha/Job-bunny. You are the second
> stage of the product pipeline; the spec is user-approved and is your contract. […] THE ONE
> DECISION YOU MUST SET UP FOR THE USER: the Hub-vs-Settings boundary (spec §7.5). Propose a
> boundary (or dissolution) as your recommended design, but present the realistic alternatives with
> what rides on each — the user rules on this at the mockup gate, through the orchestrator (me).
> […] DELIVERABLES: ux-notes.md + mockup.html per your charter (mockup rendered at the cheaper tier
> from your dossier, with a full state inventory — downstream QA drives the built app against it).

Decisions the dispatch closed and UX designed *within*: intent-oriented IA; retunable rank lists
only; one consolidated Advanced/raw area; pacing presets + advanced disclosure; connector read-only
+ Notion mirror/dryRun toggles, no breaker reset, live breaker status available; live-daemon control
buildable now with full lifecycle mechanism deferred to the BE gate; onboarding = completeness
checklist only; R15 preview conditional and must degrade gracefully.

## UX.2 Grounding done (do not replay)

- `spec.md` READ IN FULL (all 876 lines) — §7.5 options, §8 rulings 12–14, §9 the 1/5 navigation
  test, §10 Content Priority, §11 R1–R32, §12 phases, §16 scorecard + 6 weaknesses.
- `pm-brief.md`, `recon-settings-surface.md`, `personas.md` v1.3, and the prior epic's
  `run-experience-overhaul/ux-notes.md` — all READ. Inherited constraints honoured: sidebar shell
  `w-56`, persistent last-run line, urgency = weight not hue, no charting dependency, no new deps.
- **Design-scale recon delegated to executor-fast and captured as §3 of `ux-notes.md`** — colour
  tokens (light + dark literals), Tailwind-default spacing grid, type ramp, radii, component
  geometry, shell layout, state patterns. **Do not re-recon this**; §3 is the authoritative table
  and the mockup draws every value from it.

## UX.3 Design decisions taken (product-ux judgment)

- **Boundary recommendation: Option C+D executed as a rename.** `#/setup` → label **"Operate"**
  (machine scope: daemon, scheduled-run rows, LinkedIn session + breaker, doctor/completeness,
  secrets); `#/settings` → per-profile intent IA. Seam sentence: *Operate acts on the machine;
  Settings authors what the machine should do for this profile.* Route string `#/setup` kept so
  existing e2e survives. C's known flaw dissolved by splitting *schedule times* (authored, profile)
  from *schedule execution* (operated, machine).
- **Settings IA:** 12 sections in 4 groups (Aim / Runs / Output / Advanced). Renames: Filters →
  Roles & companies + Where you'll work + Skills; Resume → About you; Profile → dissolved.
- **Repeated idiom:** every Aim section is "Rules — dropped" + "Preferences — reordered". This is
  how R2 joins two documents without lying about their semantics.
- **Added beyond the numbered requirements (recorded, not silent):** the S1 "What decides your
  board" landing screen, and the merge of R21 (doctor) + R22 (completeness) into one card.
- Usability concerns from spec §16 resolved: (a) §1, (b) §6 + C2/C4, (c) §9 + C7, (d) §7 + C5,
  (e) §2 + C15 — place first, label second.

## UX.4 The open ruling for the user (mockup gate)

Recommended: **C+D + rename to "Operate"**. Alternatives presented with costs in `ux-notes.md` §1:
**A** merge into one page · **B** health-vs-config keeping both names · **C+D but keep the name
"Setup & Health"**. Cost of reversal is LOW — the §2 section inventory is boundary-independent;
only which page hosts §10's five cards changes.

## UX.5 Handoffs

- **To BE:** (a) can the board cheaply evaluate the filter-timezone *remote-only* condition that the
  cross-document conflict notice (C4) depends on — if not, R2 degrades from a join to adjacency;
  (b) R15 spike; (c) R20 mechanism; (d) `userDataDir` resolution for R18; (e) whether schedule pause
  is per-profile or machine-wide — UX designed the superset (per-profile rows + master).
- **To product-ui:** promote R26 (search) from Could if a re-run of the §9 navigation test against
  the new IA scores below 4/5; delete dead `PLACEHOLDER_COPY` at `SettingsPage.tsx:30-34`.
- **To QA:** `ux-notes.md` §12 is the state inventory and §17 is the `data-qa` id map — the built app
  is driven against both.
